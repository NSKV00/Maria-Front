import { useNavigate } from "react-router-dom"
import { Header } from "../../components/header/Header"

export const Home = ()=>{
    const navigate = useNavigate()

    return<>
    <Header />
        <main>
            <button>kcfdytgckyrtgc,</button>
        </main>
    </>
}

// Home = pagina de produtos